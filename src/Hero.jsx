const Hero = ({ blok }) => {
    console.log('blok',blok);
    
    return (
        <section>
            {/* <h1>Hello from Storyblok 🚀</h1> */}
            {/* <h1>{blok.Title}</h1>
            <p>{blok.description}</p> */}
        </section>
    );
};

export default Hero;
